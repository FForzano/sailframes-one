// Shared bullet list for E1 device info, rendered both on the landing page
// and in the device-detail info dialog (`E1InfoDialog`) — one source of
// truth for the copy instead of two hand-synced lists.
export const E1_INFO_BULLET_KEYS = [
  "sensors",
  "display",
  "mesh",
  "upload",
  "power",
  "openHardware",
] as const;
